{
  "name": "factplus-monorepo",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "license": "MIT",
  "description": "FactPlus - A fact-checking platform",
  "scripts": {
    "build": "cd backend && npm run build",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
